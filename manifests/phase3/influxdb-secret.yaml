# ============================================================
# InfluxDB Secret
# ⚠️  เปลี่ยน password และ token ก่อน deploy จริง
# ============================================================
apiVersion: v1
kind: Secret
metadata:
  name: influxdb-secret
  namespace: it
type: Opaque
stringData:
  admin-user: "admin"
  admin-password: "CHANGE_ME"
  admin-token: "CHANGE_ME"
  org: "mintpower-org"
  bucket: "opc-data"
