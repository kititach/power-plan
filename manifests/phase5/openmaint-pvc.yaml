apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-openmaint
  namespace: it
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-nvme
  resources:
    requests:
      storage: 20Gi
  selector:
    matchLabels:
      app: openmaint
